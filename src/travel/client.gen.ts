// GENERATED by scripts/generate.ts — do not edit. Run: bun run generate

import type { BrexCore } from "../core/client";
import type { RequestOptions } from "../core/options";
import type { PagePromise } from "../core/pagination";
import type { paths } from "./types.gen";

export class Trips {
  constructor(private readonly _core: BrexCore) {}

  /**
   * Get trip
   * `GET /v1/trips/{trip_id}` — requires OAuth scope: `travel.trips`, `travel.trips.readonly`
   */
  get(
    tripId: paths["/v1/trips/{trip_id}"]["get"]["parameters"]["path"]["trip_id"],
    options?: RequestOptions,
  ): Promise<
    paths["/v1/trips/{trip_id}"]["get"]["responses"]["200"]["content"]["application/json"]
  > {
    return this._core.request<
      paths["/v1/trips/{trip_id}"]["get"]["responses"]["200"]["content"]["application/json"]
    >("GET", `/v1/trips/${encodeURIComponent(tripId)}`, { options });
  }

  /**
   * Get booking
   * `GET /v1/trips/{trip_id}/bookings/{booking_id}` — requires OAuth scope: `travel.trips`, `travel.trips.readonly`
   */
  getBooking(
    tripId: paths["/v1/trips/{trip_id}/bookings/{booking_id}"]["get"]["parameters"]["path"]["trip_id"],
    bookingId: paths["/v1/trips/{trip_id}/bookings/{booking_id}"]["get"]["parameters"]["path"]["booking_id"],
    options?: RequestOptions,
  ): Promise<
    paths["/v1/trips/{trip_id}/bookings/{booking_id}"]["get"]["responses"]["200"]["content"]["application/json"]
  > {
    return this._core.request<
      paths["/v1/trips/{trip_id}/bookings/{booking_id}"]["get"]["responses"]["200"]["content"]["application/json"]
    >("GET", `/v1/trips/${encodeURIComponent(tripId)}/bookings/${encodeURIComponent(bookingId)}`, {
      options,
    });
  }

  /**
   * List trips
   * `GET /v1/trips` — requires OAuth scope: `travel.trips`, `travel.trips.readonly`
   * Await for a single page, or `for await` to iterate items across all pages.
   */
  list(
    query?: paths["/v1/trips"]["get"]["parameters"]["query"],
    options?: RequestOptions,
  ): PagePromise<paths["/v1/trips"]["get"]["responses"]["200"]["content"]["application/json"]> {
    return this._core.list<
      paths["/v1/trips"]["get"]["responses"]["200"]["content"]["application/json"]
    >("/v1/trips", { query, options });
  }

  /**
   * List trip bookings
   * `GET /v1/trips/{trip_id}/bookings` — requires OAuth scope: `travel.trips`, `travel.trips.readonly`
   * Await for a single page, or `for await` to iterate items across all pages.
   */
  listBookings(
    tripId: paths["/v1/trips/{trip_id}/bookings"]["get"]["parameters"]["path"]["trip_id"],
    query?: paths["/v1/trips/{trip_id}/bookings"]["get"]["parameters"]["query"],
    options?: RequestOptions,
  ): PagePromise<
    paths["/v1/trips/{trip_id}/bookings"]["get"]["responses"]["200"]["content"]["application/json"]
  > {
    return this._core.list<
      paths["/v1/trips/{trip_id}/bookings"]["get"]["responses"]["200"]["content"]["application/json"]
    >(`/v1/trips/${encodeURIComponent(tripId)}/bookings`, { query, options });
  }
}

export class BrexTravel {
  readonly trips: Trips;
  constructor(core: BrexCore) {
    this.trips = new Trips(core);
  }
}
